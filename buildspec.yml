version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - echo "installing something"     
  pre_build:
    commands:
      - echo "we are in the pre_build phase..."
  build:
    commands:
      - echo "We are in the build block"
      - echo "We will run some tests"
      - ls -la
      - cat index.html
      - grep -Fq "Test app for AWS CodePipeline" index.html || true
  post_build:
    commands:
      - echo "Build completed"
      
artifacts:
  files:
    - '**/*'
